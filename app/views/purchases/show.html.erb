<h2>Purchase details</h2>

<table style="width: 400px;" class="table table-striped table-bordered table-condensed">
	<tr>
		<th>Provider</th>
		<td><%user = User.find(@purchase.user_id)%>
			<a href="<%= url_for user%>"><%= user.first_name%> <%= user.last_name%></a>
		</td>
		
	</tr>
	<tr>
		<th>Purchase</th>
		<td>
			<a href="<%= url_for @offer%>"><%= @offer.name%></a>
		</td>
	</tr>
	<tr>
		<th>Date</th>
		<td>
			<%= @purchase.created_at.strftime("%d.%m.%Y %H:%M") %>
		</td>
	</tr>
	<tr>
		<th>Cost</th>
		<td>
			€<%= @purchase.quantity * @offer.price %>
		</td>
	</tr>
	
</table>
<%= link_to 'Back', purchases_path %>
